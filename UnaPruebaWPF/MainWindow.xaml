<Window x:Class="UnaPruebaWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UnaPruebaWPF"
        mc:Ignorable="d"
        Title="Página de Inicio" Height="450" Width="800">
    <Window.DataContext>
        <local:ModeloVista/>
    </Window.DataContext>
    <Window.Resources>

    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Label x:Name="Label01" Content="Nombres" HorizontalAlignment="Center" Height="40" VerticalAlignment="Center" Width="220" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" FontSize="16" FontWeight="Bold"/>
        <ScrollViewer  Grid.Row="1" Margin="10" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <ListBox Name="ListaNombres" ItemsSource="{Binding Milista}"/>
        </ScrollViewer>
        <StackPanel Grid.Column="1" Grid.Row="1" Margin="10 ">

        </StackPanel>
        <Grid Grid.Column="1" Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="EntradaTexto" TextWrapping="Wrap" Text="" Margin="10" HorizontalAlignment="Stretch" Height="40" FontSize="20" VerticalContentAlignment="Center" Cursor=""/>
            <Button x:Name="BotonHola" Content="Hola Mundo Cruel" FontSize="20" IsDefault="True" Cursor="Hand" FontWeight="Bold" BorderThickness="3,3,3,3" Grid.Column="0" Margin="10" Grid.Row="1" HorizontalAlignment="Center" Width="260" Click="ClickBoton"/>
            <Label x:Name="ElementoLista" Content="Nada elegido" HorizontalAlignment="Stretch" Height="57" Margin="10" Grid.Row="2" VerticalAlignment="Center" VerticalContentAlignment="Center" />
        </Grid>
    </Grid>
</Window>
